{
  "name": "practice",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "dependencies": {
    "axios": "^0.27.2",
    "concurrently": "^7.2.1",
    "cors": "^2.8.5",
    "dotenv": "^16.0.1",
    "express": "^4.18.1",
    "mongoose": "^6.3.6",
    "nodemon": "^2.0.16",
    "react-scripts": "^5.0.1"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "cd client && react-scripts build",
    "install": "npm ci && cd client && npm ci",
    "heroku-postbuild": "npm run install && npm run build",
    "start": "nodemon server.js",
    "client": "cd client && npm start",
    "dev": "concurrently -n 'server,client' \"npm run start\" \"npm run client\""
  },
  "author": "Harjit Karmacharya",
  "license": "ISC"
}
